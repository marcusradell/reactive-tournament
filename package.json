{
  "name": "reactive-tournament",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "@most/hold": "^1.3.1",
    "@types/es6-promise": "^0.0.32",
    "@types/express": "^4.0.34",
    "@types/node": "^6.0.46",
    "@types/ramda": "^0.0.2",
    "express": "^4.14.0",
    "most": "^1.1.0",
    "most-subject": "^5.2.0",
    "neo4j-driver": "^1.1.0-M02",
    "nodemon": "^1.11.0",
    "ramda": "^0.22.1",
    "snabbdom": "^0.5.4",
    "typescript": "next",
    "webpack": "^1.13.3"
  },
  "devDependencies": {},
  "scripts": {
    "build:clean": "rm -rf build && mkdir -p build/client",
    "build:static": "cp -r static/* build/client",
    "build": "npm run build:clean && npm run build:static && tsc --project tsconfig.json",
    "start": "npm run build && node build/server/index.js",
    "watch": "nodemon --watch server --watch client -e ts -x 'npm start'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marcusnielsen/reactive-tournament.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/marcusnielsen/reactive-tournament/issues"
  },
  "homepage": "https://github.com/marcusnielsen/reactive-tournament#readme"
}
